import { DeleteMessageChannelOptions } from "types/customTypes";

const DELETE_MESSAGE_CHANNELS: Map<string, DeleteMessageChannelOptions> =
    new Map([
        [
            "1275645370076233830",
            {
                response:
                    "please use the `/recommendation` slash command to add recommendations, TY",
                appendUser: true
            },
        ],
    ]);

export { DELETE_MESSAGE_CHANNELS };
